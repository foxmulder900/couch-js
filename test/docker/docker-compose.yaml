version: '2.0'
services:
  couchdb:
    image: couchdb:3.1.0
    ports:
      - 5984:5984
    environment:
      COUCHDB_USER: test_user
      COUCHDB_PASSWORD: test_password
    volumes:
    - ./local.ini:/opt/couchdb/etc/local.ini